h2(#installation).  Installation

Click the link for your operating system to see instructions on how to install TNAPS:

h2(#startup).  Starting up

To start up a TNAPS node, change directory as necessary to where you installed TNAPS (usually @/opt/tnaps@) and run mono with tnaps:


```bash
$ mono tnaps.exe
```
